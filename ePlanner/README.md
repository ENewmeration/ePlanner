# ePlanner

##### Install Gems
    gem install bundler
    bundle install

##### Initialize Database
    rake init

##### Run Tests
    rake test

##### Run Server (http://localhost:3000/)
    rails -s
